# Meta Pixel ID for client-side tracking (src/components/analytics/Pixel.tsx).
# Public (bundled into the client build). Optional for local dev — without it the
# pixel script loads but reports to no pixel; required in production.
NEXT_PUBLIC_META_PIXEL_ID=
